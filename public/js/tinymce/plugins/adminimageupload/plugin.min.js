tinymce.PluginManager.add('adminimageupload', function(editor, url) {
    editor.addButton('adminimageupload', {
        tooltip: 'Admin: Insert image',
        icon : 'image',
        text: 'Upload',
        onclick: function() {
            openImageModal();
        }
    });
});
